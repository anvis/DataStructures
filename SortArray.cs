using System;
using System.Collections.Generic;
using System.Text;

namespace ConsoleApp3
{
  class SortArray
  {

    //static void Main(string[] args)
    //{
    //  // int[] a = { 29, 15, 0, 98, 0, 0, 99, 0 };

    //  int[] a = { 29, 15, 0, 98, 3, 1, 99, 2};


    //  int aCount = (int) (a.Length - 1 )/ 2;

    //  for (int i = 0; i <= aCount; i++)
    //  {
    //    if (a[i] > a[aCount + i + 1])
    //    {
    //      int tmp = a[aCount + i + 1];
    //      a[aCount + i + 1] = a[i];
    //      a[i] = tmp;
    //    }
    //  }





    //    for (int i = 0; i < a.Length - 1; i++)
    //  {

    //    if (a[i] == 0)
    //    {
    //      int tmp = a[i + 1];
    //      a[i + 1] = 0;
    //      a[i] = tmp;
    //    }

    //    //int tmp = a[i];
    //    //a[i] = a[a.Length - i - 1];
    //    //a[a.Length - i - 1] = tmp;
    //  }

    //  foreach (var item in a)
    //  {
    //    Console.WriteLine(item);
    //  }

    //}
  }
}
